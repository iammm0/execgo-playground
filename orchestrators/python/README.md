# Python Orchestrator (langgraph)

该目录提供 Python 版本编排层最小模板，语义与 Go/TS 对齐。

## 安装

```bash
pip install langgraph requests
```

## 运行

```bash
python -m src.main --request ../../scenarios/basic/request.json
```
